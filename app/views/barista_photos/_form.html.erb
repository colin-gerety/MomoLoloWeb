<%= form_with(model: barista_photo, local: true) do |form| %>
  <% if barista_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(barista_photo.errors.count, "error") %> prohibited this barista_photo from being saved:</h2>

      <ul>
      <% barista_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note %>
  </div>

  <div class="field">
    <%= form.label :can_display %>
    <%= form.check_box :can_display %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
