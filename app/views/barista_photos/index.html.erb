<p id="notice"><%= notice %></p>

<h1>Barista Photos</h1>

<table>
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>Note</th>
      <th>Can display</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @barista_photos.each do |barista_photo| %>
      <tr>
        <td><%= barista_photo.image %></td>
        <td><%= barista_photo.title %></td>
        <td><%= barista_photo.note %></td>
        <td><%= barista_photo.can_display %></td>
        <td><%= link_to 'Show', barista_photo %></td>
        <td><%= link_to 'Edit', edit_barista_photo_path(barista_photo) %></td>
        <td><%= link_to 'Destroy', barista_photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Barista Photo', new_barista_photo_path %>
