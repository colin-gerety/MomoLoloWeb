<%= form_with(model: place_photo, local: true) do |form| %>
  <% if place_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(place_photo.errors.count, "error") %> prohibited this place_photo from being saved:</h2>

      <ul>
      <% place_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_field :note %>
  </div>

  <div class="field">
    <%= form.label :can_display %>
    <%= form.check_box :can_display %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
