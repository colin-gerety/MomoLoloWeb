<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :artist_phone %>
  </div>

  <div class="field">
    <%= form.label :web_site %>
    <%= form.text_field :web_site, id: :artist_web_site %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :artist_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
